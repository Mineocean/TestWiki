{
  "name": "sage",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist && staticrypt dist/docs/dev/secret/index.html -p sage123 -d dist/docs/dev/secret/ --short",
    "encrypt": "staticrypt dist/docs/dev/secret/index.html -p sage123 -d dist/docs/dev/secret/ --short"
  },
  "dependencies": {
    "@astrojs/mdx": "^5.0.4",
    "astro": "^6.2.2",
    "pagefind": "^1.5.2",
    "staticrypt": "^3.5.4"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  }
}
