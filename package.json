{
  "name": "sage",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/encrypt.mjs && pagefind --site dist && node scripts/check-build.mjs",
    "encrypt": "node scripts/encrypt.mjs",
    "typecheck": "astro check",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@astrojs/mdx": "^5.0.4",
    "astro": "^6.2.2",
    "pagefind": "^1.5.2",
    "staticrypt": "^3.5.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
